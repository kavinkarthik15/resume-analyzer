# Resume Analyser UI

React + Vite project with login (glass effect), home screen, and upload page.

The home screen mirrors the sample UI with a colourful hero section, upload card, feature grid, role readiness buttons, assistant hints, and footer. Colour palette and gradients are defined in `src/styles/global.css`.

To run:

```bash
npm install
npm run dev
```
